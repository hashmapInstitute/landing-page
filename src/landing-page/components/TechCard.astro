---
const { title, body } = Astro.props;
---

<div class="card">
  <h3>{ title }</h3>
  <p>{ body }</p>
</div>

<style>
  .card {
    background: #fff;
    border-radius: 3px;
    box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    margin: 0 0 1rem;
    padding: 1rem;
    width: calc(50% - 2.5rem);
  }

  .card h3 {
    font-size: 26px;
    margin: 0 0 0.5rem;
    color: var(--hi-purple);
  }
</style>